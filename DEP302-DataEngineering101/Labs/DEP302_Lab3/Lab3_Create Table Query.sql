CREATE TABLE TuitionDataSystem (
	last_name VARCHAR(40) NOT NULL,
	first_name VARCHAR (25) NOT NULL,
	academic_status VARCHAR (4) NOT NULL -- FGS, PGS, FAP, PAP
);

CREATE TABLE AttendanceDataSystem (
	last_name VARCHAR(35) NOT NULL,
	fist_name VARCHAR(15) NOT NULL,
	enrollment_status VARCHAR(10) NOT NULL, --2 values: "Full-time", "Part-time"
	academic_status VARCHAR(13) NOT NULL -- "Good Standing" , "
);

CREATE TABLE StudentData (
	last_name

SELECT * FROM TuitionDatabase
